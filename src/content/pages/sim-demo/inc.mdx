---
order: 1
simId: sim-demo/inc
modelId: inc
---

import SimSource from '../../../sim/components/SimSource'
import SimVis from '../../../sim/components/SimVis'
import SimCharts from '../../../sim/components/SimCharts'

# Inc — minimal simulation

**Inc** is the simplest possible simulation: a single number that increments by one on every tick (once per second).

Its purpose is to verify the full engine workflow with the smallest possible model.

Press **Start** to run. Watch the value increment in the 3D view.

<SimVis client:only="react" />

---

## Chart

The value grows linearly — one unit per second.

<SimCharts varIds={[['value']]} client:only="react" />

---

## Edit the block

Change the increment — for example `state.value + 2` or `state.value * 2`. Stage with **Ctrl+Enter**, then press **Apply & Start** in the visualization above.

<SimSource sourceIds={['inc']} autoHeight client:only="react" />

---

## Engine concepts demonstrated

| Concept | Where |
|---|---|
| Single-block model | `inc.config.ts` |
| `mapStateIn` / `mapStateOut` | slices the full state down to what this block needs |
| `tickFrequency: 1` | block runs every tick |
| TS type stripping + eval | edit the function and stage it |
| History | chart grows tick by tick; scrub slider rewinds |
| Scene handler | `inc.scene.ts` renders the number as a 3D sprite |
